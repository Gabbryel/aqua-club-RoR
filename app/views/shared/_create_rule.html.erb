<%= simple_form_for @rule  do |f| %>
    <%= f.input :body, label: 'Scrie o regulă nouă' %>
    <%= f.input :photo, as: :file %>
    <%= f.button :submit, value: 'Regulă nouă', class: 'bck-light neon-green neon-square-button' %>
<% end %>